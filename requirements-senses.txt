# Optional senses — install what you want Vidit to have. All run offline.
# Ears (offline speech-to-text + wake word). Uses the RTX 4050 through CUDA if available.
#
# Windows note: on some CPUs/wheel combos, faster-whisper can crash the whole
# process ("Windows fatal exception: access violation") the first time the model
# loads. Vidit now probes the model in a separate process first, so it degrades
# gracefully instead of crashing — but if you see the crash anyway, run:
#     pip install "ctranslate2==4.4.0"        # known-good engine on Windows
# and set  Settings -> Voice -> STT compute type -> float32.
faster-whisper>=1.0
sounddevice>=0.4
numpy>=1.24

# Voice (choose one; pyttsx3 uses Windows' built-in voices, Piper sounds far better)
pyttsx3>=2.90
piper-tts>=1.2

# Eyes (camera, presence, smiles, game-screen analysis)
opencv-python>=4.8
# face_recognition   # friends' faces — needs dlib / CMake; install only if you want it

# Files
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1
Pillow>=10.0

# Gaming
mss>=9.0
pyautogui>=0.9
