# Optional senses — install what you want Vidit to have. All run offline.
# Ears (offline speech-to-text + wake word). Uses the RTX 4050 through CUDA if available.
faster-whisper>=1.0
sounddevice>=0.4
numpy>=1.24

# Voice (choose one; pyttsx3 uses Windows' built-in voices, Piper sounds far better)
pyttsx3>=2.90
piper-tts>=1.2

# Eyes (camera, presence, smiles, game-screen analysis)
opencv-python>=4.8
# face_recognition   # friends' faces — needs dlib / CMake; install only if you want it

# Files
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1
Pillow>=10.0

# Gaming
mss>=9.0
pyautogui>=0.9
